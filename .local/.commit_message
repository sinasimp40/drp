Task #9: Bigger GIF logo, heartbeat grace, and splash preview page

Logo sizing:
- Changed splash logo from 100x100 square to 480x100 wide bounding box
  (LOGO_MAX_W=480, LOGO_MAX_H=100) so GIFs fill the banner area.
- Both GIF (QMovie) and PNG (QPixmap) scale with KeepAspectRatio.
- Logo vertically centered within the bounding box.

Heartbeat grace:
- LICENSE_OFFLINE_GRACE changed from 3 to 18 (~3 min tolerance).

Splash preview page:
- Added /splash_preview route (admin-only) that renders an HTML replica
  of the launcher splash screen with the actual splash_logo.gif/png.
- Added /splash_logo_file route to serve the splash logo file.
- GIF animates in the preview exactly as it would in the launcher.
- Progress bar animates to simulate the loading experience.
- Added "Splash Preview" button to the Builds page header.
- New template: license_server/templates/splash_preview.html

Files modified: launcher.py, license_server/server.py,
  license_server/templates/builds.html
Files added: license_server/templates/splash_preview.html
